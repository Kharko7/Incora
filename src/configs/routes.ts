interface Routes {
  pathToPagination: string;
  pathToTask1: string;
  pathToSettings: string;
  pathToSetSettings: string;
}

const routes: Routes = {
  pathToTask1: '/task1',
  pathToPagination: '/pagination',
  pathToSettings: '/settings',
  pathToSetSettings: '/set-settings',
}

export default routes